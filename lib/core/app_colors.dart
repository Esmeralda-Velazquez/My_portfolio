import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primary = Color(0xFF79A3D9);
  static const Color secondary = Color(0xFFF27777);
  static const Color tertiary = Color(0xFFF22727);

  // Colores de fondo
  static const Color background = Color(0xFF0D0D0D);
  static const Color surface = Color(0xFFF2DBD5);


  // Colores de texto

  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFE0E0E0);

  // Colores de alerta
  static const Color error = Color(0xFFB00020);
}
